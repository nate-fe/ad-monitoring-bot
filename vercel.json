{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "npm ci && npx playwright install --with-deps chromium",
  "buildCommand": "node ./scripts/prepare-vercel-build.js",
  "outputDirectory": "dist",
  "crons": [
    {
      "path": "/api/cron/redeploy",
      "schedule": "0 * * * *"
    }
  ]
}
